{
  "name": "order-process-linked-list",
  "private": true,
  "version": "1.0.0",
  "description": "Data structures case study: a doubly linked list built from scratch, used to model an order/quote CRUD process.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vite": "^5.4.0"
  }
}
